---
description: Generate a full week of cross-platform content from the pillars
argument-hint: [optional theme or POV to feature]
---

Generate a full week of content for Dr. Philip Babalola. Feature focus (if given): $ARGUMENTS

Process:
1. Read `knowledge-base/08-content-pillars.md` and check `content/` and `calendar/content-calendar.md` for what has been used recently — do not repeat last week's POVs.
2. Plan the week to hit at least 4 pillars across platforms at PB's cadence:
   - 2× LinkedIn posts (different pillars)
   - 1× X thread + 3× standalone one-liners
   - 2× Instagram (1 carousel + 1 quote card or story post)
   - 1× short-form video script
   - 1× newsletter edition
3. Present the week plan as a table (day, platform, pillar, POV, hook) BEFORE drafting.
4. Then draft every piece using the matching template and slash-command process, saving each to its `content/` folder with proper frontmatter.
5. Update `calendar/content-calendar.md` with the planned week.
6. Finish with a summary: what was created, where, and any `[NEEDS PB INPUT]` flags across the batch.
