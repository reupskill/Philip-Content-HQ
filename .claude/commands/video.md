---
description: Draft a video script (short-form or long-form) in PB's voice
argument-hint: [topic] [--long]
---

Draft a video script for Dr. Philip Babalola on: $ARGUMENTS

Process:
1. Read `knowledge-base/02-voice-and-tone.md`, `knowledge-base/05-signature-phrases.md`, and `templates/video-script.md`.
2. Default to Format A (45–90s short-form). Use Format B (3–8 min) if `--long` is passed or the topic is a founder story.
3. One POV per video from `knowledge-base/03-core-beliefs-pov.md`; include at least one verbatim signature phrase; write delivery notes (`[pause]`, `[direct to camera]`) into the script.
4. Include on-screen text callouts and the closing CTA line.
5. Save to `content/video-scripts/YYYY-MM-DD-slug.md` with frontmatter: pillar, POV, format, est-runtime, status: draft.

Write for the ear: short sentences, natural rhythm. No hype lines. Never invent facts.
