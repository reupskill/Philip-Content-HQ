# --- Required ---

# Claude API key from https://platform.claude.com
ANTHROPIC_API_KEY=sk-ant-...

# Exactly the emails allowed to sign in, comma-separated (2 people)
ALLOWED_EMAILS=philipbabs29@gmail.com,ceocontent@uvest.team

# The shared password both users type to sign in (with their allowed email).
# Pick something long. Change it here to rotate access.
LOGIN_PASSWORD=

# Random secret for signing session cookies.
# Generate one with:  openssl rand -base64 32
AUTH_SECRET=
